# Getting Started & Setup Guide 🚀

Follow this comprehensive guide to set up your local development environment for **ENIAD Explainable AI (XAI) Suite**.

---

## 📋 Prerequisites

Ensure your system meets the required runtime dependencies:
- **Git** (version 2.30+)
- **Python 3.10+** (with `pip` and virtual environment support)
- **Java JDK 17+** (for Java / JADE modules)
- **Android Studio** (for Android mobile modules)

---

## 📥 Repository Setup

1. **Clone the repository**:
   ```bash
   git clone https://github.com/Bosaj/eniad-explainable-ai.git
   cd eniad-explainable-ai
   ```

2. **Virtual Environment Setup (Python modules)**:
   ```bash
   python -m venv venv
   # On Windows:
   .\venv\Scripts\activate
   # On Linux/macOS:
   source venv/bin/activate
   ```

3. **Dependency Installation**:
   Install needed packages or launch notebooks in VS Code or JupyterLab.

---

## 🧪 Validation

Verify your installation by running the repository check or executing any of the introductory laboratory notebooks.
